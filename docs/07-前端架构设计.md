# 前端架构设计

## 基于生成式 AI 的儿童绘本智能创作与图像生成系统

**版本**: 2.0  
**日期**: 2026-09-19  
**作者**: StoryCanvas 开发团队

> **v2.0 修订**：范围收敛为**用户管理**与**智能图像生成**两大子系统，**在线编辑子系统整体放弃**。
> 本版已移除 Fabric.js 依赖、画布编辑器组件、编辑器状态机、页面排版与图片上传等章节。权威方案见 `docs/16-项目实施方案.md`。

---

## 1. 技术栈

### 1.1 核心框架
- **React 18**: 用户界面框架
- **TypeScript**: 类型安全
- **Vite**: 构建工具

### 1.2 UI 相关
- **Ant Design 5.x**: UI 组件库
- **Tailwind CSS**: 原子化 CSS

### 1.3 状态管理
- **Zustand**: 轻量级状态管理

### 1.4 路由
- **React Router 6**: 客户端路由

### 1.5 HTTP 客户端
- **Axios**: HTTP 请求

### 1.6 工具库
- **dayjs**: 日期处理
- **lodash**: 工具函数

> **已移除**：Fabric.js（随在线编辑子系统一并放弃）。

---

## 2. 项目目录结构

```
frontend/
├── public/                        # 静态资源
├── src/
│   ├── api/                       # API 请求层
│   │   ├── client.ts              # Axios 实例
│   │   ├── auth.ts                # 认证 API
│   │   ├── users.ts               # 用户 API
│   │   ├── books.ts               # 绘本 API
│   │   ├── generation.ts          # AI 生成 API
│   │   └── images.ts              # 生成图片 API
│   ├── components/                # 可复用组件
│   │   ├── common/                # 通用组件（Button/Modal/Loading/EmptyState）
│   │   ├── ai/                    # AI 相关组件
│   │   │   ├── GenerationForm.tsx      # 生成表单
│   │   │   ├── StyleSelector.tsx       # 风格选择
│   │   │   ├── GenerationProgress.tsx  # 生成进度
│   │   │   ├── ResultPreview.tsx       # 结果预览
│   │   │   └── GenerationHistory.tsx   # 生成历史（加分项）
│   │   ├── book/                  # 绘本相关组件
│   │   │   ├── BookCard.tsx       # 绘本卡片
│   │   │   └── BookList.tsx       # 绘本列表
│   │   └── layout/                # 布局组件
│   │       ├── Header.tsx
│   │       └── Sidebar.tsx
│   ├── hooks/                     # 自定义 Hooks
│   │   ├── useAuth.ts             # 认证 Hook
│   │   ├── useBooks.ts            # 绘本 Hook
│   │   ├── useGeneration.ts       # AI 生成 Hook
│   │   └── useDebounce.ts         # 防抖 Hook
│   ├── pages/                     # 页面组件
│   │   ├── auth/                  # 认证页面
│   │   │   ├── Login.tsx
│   │   │   └── Register.tsx
│   │   ├── books/                 # 绘本页面
│   │   │   ├── BookList.tsx
│   │   │   └── BookDetail.tsx
│   │   ├── generation/            # 生成页面
│   │   │   └── Generate.tsx       # AI 生成与结果预览
│   │   ├── profile/               # 个人中心
│   │   │   └── Profile.tsx
│   │   └── home/                  # 首页
│   │       └── Home.tsx
│   ├── stores/                    # 状态管理
│   │   ├── authStore.ts           # 认证状态
│   │   ├── bookStore.ts           # 绘本状态
│   │   └── uiStore.ts             # UI 状态
│   ├── types/                     # TypeScript 类型
│   ├── utils/                     # 工具函数
│   ├── styles/                    # 全局样式
│   ├── App.tsx                    # 根组件
│   └── main.tsx                   # 入口文件
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
├── .env.example
└── README.md
```

> **已移除**：`components/editor/`、`pages/editor/`、`stores/editorStore.ts`、`hooks/useCanvas.ts`（画布编辑相关）。

---

## 3. 路由设计

### 3.1 路由配置

```typescript
// src/App.tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { useAuthStore } from './stores/authStore';

import Login from './pages/auth/Login';
import Register from './pages/auth/Register';
import Home from './pages/home/Home';
import BookList from './pages/books/BookList';
import BookDetail from './pages/books/BookDetail';
import Generate from './pages/generation/Generate';
import Profile from './pages/profile/Profile';

const ProtectedRoute = ({ children }: { children: React.ReactNode }) => {
  const { isAuthenticated } = useAuthStore();
  return isAuthenticated ? <>{children}</> : <Navigate to="/login" />;
};

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* 公开路由 */}
        <Route path="/login" element={<Login />} />
        <Route path="/register" element={<Register />} />

        {/* 受保护路由 */}
        <Route path="/" element={<ProtectedRoute><Home /></ProtectedRoute>} />
        <Route path="/books" element={<ProtectedRoute><BookList /></ProtectedRoute>} />
        <Route path="/books/:bookId" element={<ProtectedRoute><BookDetail /></ProtectedRoute>} />
        <Route path="/generate" element={<ProtectedRoute><Generate /></ProtectedRoute>} />
        <Route path="/profile" element={<ProtectedRoute><Profile /></ProtectedRoute>} />

        <Route path="*" element={<Navigate to="/" />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

### 3.2 路由列表

| 路径 | 页面 | 说明 | 认证 |
|------|------|------|------|
| `/login` | Login | 登录页 | 否 |
| `/register` | Register | 注册页 | 否 |
| `/` | Home | 首页 | 是 |
| `/books` | BookList | 绘本列表 | 是 |
| `/books/:bookId` | BookDetail | 绘本详情 | 是 |
| `/generate` | Generate | AI 生成与结果预览（**核心页面**） | 是 |
| `/profile` | Profile | 个人中心 | 是 |

> 原 `/editor/:bookId`（编辑器）路由已移除。

---

## 4. 状态管理

### 4.1 认证状态

```typescript
// src/stores/authStore.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import { User, AuthResponse } from '../types/auth';

interface AuthState {
  user: User | null;
  accessToken: string | null;
  refreshToken: string | null;
  isAuthenticated: boolean;

  login: (response: AuthResponse) => void;
  logout: () => void;
  updateUser: (user: Partial<User>) => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      accessToken: null,
      refreshToken: null,
      isAuthenticated: false,

      login: (response) => set({
        user: response.user,
        accessToken: response.access_token,
        refreshToken: response.refresh_token,
        isAuthenticated: true,
      }),

      logout: () => set({
        user: null,
        accessToken: null,
        refreshToken: null,
        isAuthenticated: false,
      }),

      updateUser: (userData) => set((state) => ({
        user: state.user ? { ...state.user, ...userData } : null,
      })),
    }),
    {
      name: 'auth-storage',
      partialize: (state) => ({
        user: state.user,
        accessToken: state.accessToken,
        refreshToken: state.refreshToken,
        isAuthenticated: state.isAuthenticated,
      }),
    }
  )
);
```

> **安全提示**：`auth-storage` 会写入 localStorage，**只允许存放 Token 与基础用户信息，禁止存放密码或任何服务端密钥**。

### 4.2 生成状态

```typescript
// src/stores/generationStore.ts
import { create } from 'zustand';
import { TaskStatus, TaskResult, GenerationRequest } from '../types/generation';

interface GenerationState {
  currentTask: TaskResult | null;
  history: TaskResult[];
  polling: boolean;

  setCurrentTask: (task: TaskResult | null) => void;
  setHistory: (history: TaskResult[]) => void;
  setPolling: (polling: boolean) => void;
  reset: () => void;
}

export const useGenerationStore = create<GenerationState>()((set) => ({
  currentTask: null,
  history: [],
  polling: false,

  setCurrentTask: (task) => set({ currentTask: task }),
  setHistory: (history) => set({ history }),
  setPolling: (polling) => set({ polling }),
  reset: () => set({ currentTask: null, polling: false }),
}));
```

> 原 `editorStore`（画布、元素、撤销/重做）已随在线编辑子系统一并移除。

### 4.3 UI 状态

```typescript
// src/stores/uiStore.ts
import { create } from 'zustand';

interface UIState {
  sidebarCollapsed: boolean;
  loading: boolean;
  theme: 'light' | 'dark';

  toggleSidebar: () => void;
  setLoading: (loading: boolean) => void;
  setTheme: (theme: 'light' | 'dark') => void;
}

export const useUIStore = create<UIState>()((set) => ({
  sidebarCollapsed: false,
  loading: false,
  theme: 'light',

  toggleSidebar: () => set((state) => ({ sidebarCollapsed: !state.sidebarCollapsed })),
  setLoading: (loading) => set({ loading }),
  setTheme: (theme) => set({ theme }),
}));
```

---

## 5. API 请求封装

### 5.1 Axios 实例

```typescript
// src/api/client.ts
import axios, { AxiosInstance, AxiosError, InternalAxiosRequestConfig } from 'axios';
import { useAuthStore } from '../stores/authStore';
import { message } from 'antd';

const client: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080/api/v1',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 请求拦截器：自动携带 JWT
client.interceptors.request.use(
  (config: InternalAxiosRequestConfig) => {
    const { accessToken } = useAuthStore.getState();
    if (accessToken) {
      config.headers.Authorization = `Bearer ${accessToken}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器：401 自动刷新 Token
client.interceptors.response.use(
  (response) => response,
  async (error: AxiosError) => {
    const originalRequest = error.config as any;

    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;

      try {
        const { refreshToken, login, logout } = useAuthStore.getState();

        if (refreshToken) {
          const response = await axios.post(
            `${import.meta.env.VITE_API_BASE_URL}/auth/refresh`,
            { refresh_token: refreshToken }
          );

          const { access_token, refresh_token } = response.data.data;
          login({
            user: useAuthStore.getState().user!,
            access_token,
            refresh_token,
          });

          originalRequest.headers.Authorization = `Bearer ${access_token}`;
          return client(originalRequest);
        }
      } catch (refreshError) {
        useAuthStore.getState().logout();
        window.location.href = '/login';
      }
    }

    const errorMessage = (error.response?.data as any)?.message || '请求失败';
    message.error(errorMessage);

    return Promise.reject(error);
  }
);

export default client;
```

### 5.2 API 模块

```typescript
// src/api/auth.ts
import client from './client';
import { AuthResponse, LoginRequest, RegisterRequest } from '../types/auth';

export const authApi = {
  register: (data: RegisterRequest) =>
    client.post<{ data: AuthResponse }>('/auth/register', data),

  login: (data: LoginRequest) =>
    client.post<{ data: AuthResponse }>('/auth/login', data),

  logout: () => client.post('/auth/logout'),

  refreshToken: (refreshToken: string) =>
    client.post<{ data: { access_token: string; refresh_token: string } }>(
      '/auth/refresh',
      { refresh_token: refreshToken }
    ),
};

// src/api/books.ts
import client from './client';
import { Book, CreateBookRequest } from '../types/book';

export const booksApi = {
  getBooks: (params?: { page?: number; limit?: number; status?: string }) =>
    client.get<{ data: { items: Book[]; total: number } }>('/users/me/books', { params }),

  getBook: (bookId: string) =>
    client.get<{ data: Book }>(`/books/${bookId}`),

  createBook: (data: CreateBookRequest) =>
    client.post<{ data: Book }>('/books', data),

  updateBook: (bookId: string, data: Partial<Book>) =>
    client.put<{ data: Book }>(`/books/${bookId}`, data),

  deleteBook: (bookId: string) =>
    client.delete(`/books/${bookId}`),
};

// src/api/generation.ts
import client from './client';
import { GenerationRequest, GenerationResponse, TaskResult } from '../types/generation';

export const generationApi = {
  createTask: (data: GenerationRequest) =>
    client.post<{ data: GenerationResponse }>('/generation', data),

  getTaskStatus: (taskId: string) =>
    client.get<{ data: TaskResult }>(`/generation/${taskId}`),

  regenerate: (taskId: string, params?: Partial<GenerationRequest>) =>
    client.post<{ data: GenerationResponse }>(`/generation/${taskId}/regenerate`, params),

  getHistory: (params?: { page?: number; limit?: number; book_id?: string }) =>
    client.get<{ data: { items: TaskResult[]; total: number } }>('/generation/history', { params }),
};

// src/api/images.ts
import client from './client';
import { GeneratedImage } from '../types/image';

export const imagesApi = {
  getImage: (imageId: string) =>
    client.get<{ data: GeneratedImage }>(`/images/${imageId}`),

  listImages: (params?: { page?: number; limit?: number; book_id?: string }) =>
    client.get<{ data: { items: GeneratedImage[]; total: number } }>('/images', { params }),

  // 加分项：单张图片下载
  downloadUrl: (imageId: string) =>
    `${import.meta.env.VITE_API_BASE_URL}/images/${imageId}/download`,
};
```

> 原 `pages.ts` / `elements.ts` / `export.ts`（画布、元素、PDF 导出）已移除。

---

## 6. AI 生成界面（核心）

### 6.1 生成表单组件

```typescript
// src/components/ai/GenerationForm.tsx
import React from 'react';
import { Form, Input, Select, Button, Card, message } from 'antd';
import { ArtStyle, GenerationRequest } from '../../types/generation';
import { useGeneration } from '../../hooks/useGeneration';

const { TextArea } = Input;
const { Option } = Select;

interface GenerationFormProps {
  bookId?: string;
  onCreated: (taskId: string) => void;
}

const GenerationForm: React.FC<GenerationFormProps> = ({ bookId, onCreated }) => {
  const [form] = Form.useForm();
  const { createTask, loading } = useGeneration();

  const handleSubmit = async (values: GenerationRequest) => {
    try {
      const result = await createTask({ ...values, book_id: bookId });
      message.success('图像生成任务已创建');
      onCreated(result.task_id);
    } catch (error) {
      message.error('生成失败，请重试');
    }
  };

  return (
    <Card title="AI 图像生成">
      <Form
        form={form}
        layout="vertical"
        onFinish={handleSubmit}
        initialValues={{ style: ArtStyle.CARTOON }}
      >
        <Form.Item
          name="text"
          label="故事描述"
          rules={[{ required: true, message: '请输入故事描述' }]}
        >
          <TextArea
            rows={4}
            maxLength={2000}
            showCount
            placeholder="例如：小女孩在雨后的森林里发现了一只受伤的小鸟。"
          />
        </Form.Item>

        <Form.Item
          name="style"
          label="艺术风格"
          rules={[{ required: true, message: '请选择艺术风格' }]}
        >
          <Select placeholder="选择风格">
            <Option value={ArtStyle.CARTOON}>卡通风格</Option>
            <Option value={ArtStyle.WATERCOLOR}>水彩风格</Option>
            <Option value={ArtStyle.OIL_PAINTING}>油画风格</Option>
            <Option value={ArtStyle.ANIME}>动漫风格</Option>
          </Select>
        </Form.Item>

        <Form.Item>
          <Button type="primary" htmlType="submit" loading={loading} block>
            生成图像
          </Button>
        </Form.Item>
      </Form>
    </Card>
  );
};

export default GenerationForm;
```

### 6.2 生成进度组件（轮询任务状态）

```typescript
// src/components/ai/GenerationProgress.tsx
import React, { useEffect, useState } from 'react';
import { Progress, Card, Tag, Space, Button } from 'antd';
import { CheckCircleOutlined, CloseCircleOutlined, SyncOutlined } from '@ant-design/icons';
import { TaskStatus, TaskResult } from '../../types/generation';
import client from '../../api/client';

interface GenerationProgressProps {
  taskId: string;
  onComplete: (result: TaskResult) => void;
  onError: (error: string) => void;
  onRegenerate: () => void;
}

const GenerationProgress: React.FC<GenerationProgressProps> = ({
  taskId,
  onComplete,
  onError,
  onRegenerate,
}) => {
  const [task, setTask] = useState<TaskResult | null>(null);

  useEffect(() => {
    let timer: ReturnType<typeof setInterval> | null = null;

    const pollTask = async () => {
      try {
        const response = await client.get<{ data: TaskResult }>(`/generation/${taskId}`);
        const data = response.data.data;
        setTask(data);

        if (data.status === TaskStatus.COMPLETED) {
          if (timer) clearInterval(timer);
          onComplete(data);
        } else if (data.status === TaskStatus.FAILED || data.status === TaskStatus.CANCELLED) {
          if (timer) clearInterval(timer);
          onError(data.error_message || '生成失败');
        }
      } catch (error) {
        console.error('轮询任务状态失败:', error);
      }
    };

    pollTask();
    timer = setInterval(pollTask, 2000);

    return () => {
      if (timer) clearInterval(timer);
    };
  }, [taskId, onComplete, onError]);

  const getStatusTag = () => {
    switch (task?.status) {
      case TaskStatus.PENDING:
        return <Tag icon={<SyncOutlined spin />} color="processing">等待中</Tag>;
      case TaskStatus.PROCESSING:
        return <Tag icon={<SyncOutlined spin />} color="processing">生成中</Tag>;
      case TaskStatus.COMPLETED:
        return <Tag icon={<CheckCircleOutlined />} color="success">已完成</Tag>;
      case TaskStatus.FAILED:
        return <Tag icon={<CloseCircleOutlined />} color="error">失败</Tag>;
      case TaskStatus.CANCELLED:
        return <Tag color="default">已取消</Tag>;
      default:
        return null;
    }
  };

  const getProgress = () => {
    switch (task?.status) {
      case TaskStatus.PENDING: return 10;
      case TaskStatus.PROCESSING: return 50;
      case TaskStatus.COMPLETED:
      case TaskStatus.FAILED: return 100;
      default: return 0;
    }
  };

  return (
    <Card title="生成进度">
      <Space direction="vertical" style={{ width: '100%' }}>
        <div>{getStatusTag()}</div>
        <Progress
          percent={getProgress()}
          status={task?.status === TaskStatus.FAILED ? 'exception' : 'active'}
        />
        {task?.status === TaskStatus.COMPLETED && task.result && (
          <div className="generation-result">
            <img
              src={task.result.image_url}
              alt="生成结果"
              style={{ maxWidth: '100%', borderRadius: '8px' }}
            />
          </div>
        )}
        {(task?.status === TaskStatus.FAILED || task?.status === TaskStatus.COMPLETED) && (
          <Button onClick={onRegenerate}>重新生成</Button>
        )}
      </Space>
    </Card>
  );
};

export default GenerationProgress;
```

---

## 7. 绘本管理与结果预览

### 7.1 绘本列表组件

```typescript
// src/components/book/BookList.tsx
import React from 'react';
import { Card, Row, Col, Empty, Pagination } from 'antd';
import { useNavigate } from 'react-router-dom';
import { Book } from '../../types/book';

interface BookListProps {
  books: Book[];
  total: number;
  page: number;
  pageSize: number;
  onPageChange: (page: number) => void;
}

const BookList: React.FC<BookListProps> = ({ books, total, page, pageSize, onPageChange }) => {
  const navigate = useNavigate();

  if (books.length === 0) {
    return <Empty description="还没有绘本，去创建一本吧" />;
  }

  return (
    <div>
      <Row gutter={[16, 16]}>
        {books.map((book) => (
          <Col key={book.id} xs={24} sm={12} md={8} lg={6}>
            <Card
              hoverable
              cover={
                book.cover_image
                  ? <img alt={book.title} src={book.cover_image} />
                  : null
              }
              onClick={() => navigate(`/books/${book.id}`)}
            >
              <Card.Meta title={book.title} description={book.description || '暂无描述'} />
            </Card>
          </Col>
        ))}
      </Row>
      <div style={{ marginTop: 16, textAlign: 'right' }}>
        <Pagination
          current={page}
          pageSize={pageSize}
          total={total}
          onChange={onPageChange}
        />
      </div>
    </div>
  );
};

export default BookList;
```

### 7.2 生成页面

```typescript
// src/pages/generation/Generate.tsx
import React, { useState } from 'react';
import { useSearchParams } from 'react-router-dom';
import { Typography, Divider } from 'antd';
import GenerationForm from '../../components/ai/GenerationForm';
import GenerationProgress from '../../components/ai/GenerationProgress';

const { Title } = Typography;

const Generate: React.FC = () => {
  const [searchParams] = useSearchParams();
  const bookId = searchParams.get('bookId') || undefined;
  const [taskId, setTaskId] = useState<string | null>(null);

  return (
    <div className="container mx-auto px-4 py-8">
      <Title level={3}>AI 图像生成</Title>
      <GenerationForm bookId={bookId} onCreated={setTaskId} />

      {taskId && (
        <>
          <Divider />
          <GenerationProgress
            taskId={taskId}
            onComplete={() => {/* 刷新历史/展示结果 */}}
            onError={(msg) => {/* 展示错误 */}}
            onRegenerate={() => setTaskId(null)}
          />
        </>
      )}
    </div>
  );
};

export default Generate;
```

---

## 8. 数据结构设计

```typescript
// src/types/generation.ts
export enum TaskStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
}

export enum ArtStyle {
  CARTOON = 'cartoon',
  WATERCOLOR = 'watercolor',
  OIL_PAINTING = 'oil_painting',
  ANIME = 'anime',
  PENCIL_SKETCH = 'pencil_sketch',
}

export interface GenerationRequest {
  text: string;
  book_id?: string;
  page_id?: string;
  style: ArtStyle;
  parameters?: Record<string, any>;
}

export interface GenerationResponse {
  task_id: string;
  status: TaskStatus;
  created_at: string;
}

export interface TaskResult {
  task_id: string;
  status: TaskStatus;
  original_text: string;
  prompt: string;
  parsed_data?: ParsedData;
  result?: { image_url: string; width: number; height: number };
  error_message?: string;
  created_at: string;
  completed_at?: string | null;
}

export interface ParsedData {
  characters: Array<{ name: string; description?: string; appearance?: string }>;
  scene: { location?: string; time?: string; weather?: string; mood?: string };
  objects: string[];
  actions: string[];
  emotions: string[];
  style: Record<string, any>;
}
```

> 原 `CanvasData` / `PageElement` / `FabricObject` 等画布数据结构已移除；`book_pages` 仅作为生成的页容器（`page_number`、`title`、`thumbnail`）。

---

## 9. 错误处理

```typescript
// src/components/common/ErrorBoundary.tsx
import React, { Component, ErrorInfo, ReactNode } from 'react';
import { Result, Button } from 'antd';

interface Props { children: ReactNode; }
interface State { hasError: boolean; error: Error | null; }

class ErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: ErrorInfo) {
    // 注意：日志中不得包含密码、Token、密钥
    console.error('组件错误:', error, errorInfo);
  }

  handleRetry = () => this.setState({ hasError: false, error: null });

  render() {
    if (this.state.hasError) {
      return (
        <Result
          status="error"
          title="页面出错了"
          subTitle={this.state.error?.message || '未知错误'}
          extra={<Button type="primary" onClick={this.handleRetry}>重试</Button>}
        />
      );
    }
    return this.props.children;
  }
}

export default ErrorBoundary;
```

---

## 10. JWT 与敏感信息处理

### 10.1 Token 存储策略

1. **Access Token**: 存储在内存中（Zustand 状态）
2. **Refresh Token**: 存储在 localStorage（通过 Zustand persist）
3. **自动刷新**: 401 错误时自动刷新 Token

### 10.2 安全要求

- **不在 URL 中传递 Token**
- **禁止在前端存储密码**：密码仅在登录/注册表单提交时传输，不做任何本地留存
- **禁止在前端存储服务端密钥**（JWT Secret、DB 口令、外部 API Key）
- **不在控制台/日志打印 Token、密码与 Authorization 头**
- 退出登录时清空本地 Token 并跳转登录页
- 生产环境强制 HTTPS，避免 Token 明文传输
- 使用 HttpOnly Cookie 承载 Refresh Token 为更优方案（可选升级）

---

## 11. 图片下载（加分项）

```typescript
// src/utils/download.ts
export const downloadImage = (url: string, filename: string) => {
  const a = document.createElement('a');
  a.href = url;
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
};
```

> 原「图片上传」功能依赖在线编辑子系统，本轮已移除；导出降级为**单张图片下载**（加分项），不做 PDF 合成。

---

**文档结束**
